<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - CSE Motors</title>
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <style>
        body {
            background: linear-gradient(135deg, #f8fafc 0%, #e2e8f0 100%);
            min-height: 100vh;
            font-family: 'Segoe UI', system-ui, sans-serif;
        }

        main {
            max-width: 600px;
            margin: 60px auto;
            padding: 0 20px;
        }

        .delete-container {
            background: white;
            border-radius: 20px;
            padding: 40px;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.1);
            border: 1px solid #e2e8f0;
            text-align: center;
        }

        .warning-icon {
            font-size: 4rem;
            color: #dc2626;
            margin-bottom: 20px;
        }

        h1 {
            color: #0c1022;
            font-family: Raleway, system-ui, sans-serif;
            font-weight: 800;
            font-size: 2rem;
            margin-bottom: 15px;
        }

        .vehicle-info {
            background: #fef2f2;
            border: 2px solid #fecaca;
            border-radius: 12px;
            padding: 20px;
            margin: 30px 0;
            text-align: left;
        }

        .vehicle-info h3 {
            color: #991b1b;
            margin: 0 0 15px 0;
            font-size: 1.3rem;
        }

        .vehicle-details {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 15px;
            font-size: 0.95rem;
        }

        .detail-item {
            display: flex;
            justify-content: space-between;
            padding: 8px 0;
            border-bottom: 1px solid #fecaca;
        }

        .detail-label {
            font-weight: 600;
            color: #7f1d1d;
        }

        .detail-value {
            color: #0c1022;
        }

        .warning-message {
            background: #fffbeb;
            border: 2px solid #fde68a;
            border-radius: 12px;
            padding: 20px;
            margin-bottom: 30px;
            color: #92400e;
            font-weight: 600;
        }

        .button-group {
            display: flex;
            gap: 20px;
            justify-content: center;
            margin-top: 30px;
        }

        .btn-delete, .btn-cancel {
            padding: 16px 32px;
            border-radius: 12px;
            font-size: 1.1rem;
            font-weight: 700;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
            transition: all 0.3s ease;
            border: none;
            cursor: pointer;
            min-width: 180px;
        }

        .btn-delete {
            background: linear-gradient(135deg, #dc2626, #b91c1c);
            color: white;
            box-shadow: 0 10px 30px rgba(220, 38, 38, 0.3);
        }

        .btn-delete:hover {
            background: linear-gradient(135deg, #b91c1c, #991b1b);
            transform: translateY(-3px);
            box-shadow: 0 15px 40px rgba(220, 38, 38, 0.4);
        }

        .btn-cancel {
            background: #f1f5f9;
            color: #475569;
            border: 2px solid #cbd5e1;
        }

        .btn-cancel:hover {
            background: #e2e8f0;
            transform: translateY(-3px);
            border-color: #94a3b8;
        }

        /* Flash Messages */
        .flash-message {
            padding: 20px;
            border-radius: 12px;
            margin-bottom: 30px;
            font-weight: 600;
            border: 2px solid;
            position: relative;
            padding-left: 60px;
        }

        .flash-message::before {
            font-family: 'Font Awesome 6 Free';
            font-weight: 900;
            position: absolute;
            left: 20px;
            top: 50%;
            transform: translateY(-50%);
            font-size: 1.3rem;
        }

        .flash-message.success {
            background: #f0fdf4;
            color: #166534;
            border-color: #bbf7d0;
        }

        .flash-message.success::before {
            content: '\f00c';
        }

        .flash-message.error {
            background: #fef2f2;
            color: #991b1b;
            border-color: #fecaca;
        }

        .flash-message.error::before {
            content: '\f06a';
        }

        /* Error Display */
        .errors {
            background: #fef2f2;
            border: 2px solid #fecaca;
            border-radius: 12px;
            padding: 20px;
            margin-bottom: 30px;
        }

        .errors ul {
            margin: 0;
            padding-left: 20px;
            color: #991b1b;
        }

        .errors li {
            margin-bottom: 8px;
            font-weight: 600;
        }

        .errors li:last-child {
            margin-bottom: 0;
        }

        @media (max-width: 767px) {
            main {
                margin: 30px auto;
                padding: 0 15px;
            }

            .delete-container {
                padding: 30px 20px;
            }

            .vehicle-details {
                grid-template-columns: 1fr;
            }

            .button-group {
                flex-direction: column;
                align-items: center;
            }

            .btn-delete, .btn-cancel {
                width: 100%;
            }
        }
    </style>
</head>
<body>
    <main>
        <div class="delete-container">
            <% if (typeof message !== 'undefined' && message) { %>
                <div class="flash-message <%= typeof message.type !== 'undefined' ? message.type : 'error' %>">
                    <%= typeof message.message !== 'undefined' ? message.message : message %>
                </div>
            <% } %>
            
            <!-- Errors -->
            <% if (errors) { %>
                <div class="errors">
                    <ul>
                        <% errors.forEach(error => { %>
                            <li><%= error %></li>
                        <% }) %>
                    </ul>
                </div>
            <% } %>
            
            <div class="warning-icon">
                <i class="fas fa-exclamation-triangle"></i>
            </div>
            
            <h1><%= title %></h1>
            <p>Are you sure you want to delete this vehicle? This action cannot be undone.</p>
            
            <% if (typeof invData !== 'undefined' && invData) { %>
            <div class="vehicle-info">
                <h3><%= invData.inv_make %> <%= invData.inv_model %> (<%= invData.inv_year %>)</h3>
                <div class="vehicle-details">
                    <div class="detail-item">
                        <span class="detail-label">Classification:</span>
                        <span class="detail-value"><%= invData.classification_name || 'N/A' %></span>
                    </div>
                    <div class="detail-item">
                        <span class="detail-label">Price:</span>
                        <span class="detail-value">$<%= parseFloat(invData.inv_price).toLocaleString('en-US', {minimumFractionDigits: 2}) %></span>
                    </div>
                    <div class="detail-item">
                        <span class="detail-label">Mileage:</span>
                        <span class="detail-value"><%= parseInt(invData.inv_miles).toLocaleString() %> miles</span>
                    </div>
                    <div class="detail-item">
                        <span class="detail-label">Color:</span>
                        <span class="detail-value"><%= invData.inv_color %></span>
                    </div>
                </div>
            </div>
            <% } else { %>
                <div class="warning-message">
                    <i class="fas fa-exclamation-circle"></i>
                    <strong>Warning:</strong> Vehicle information not available.
                </div>
            <% } %>
            
            <div class="warning-message">
                <i class="fas fa-exclamation-circle"></i>
                <strong>!!Warning:</strong> This action will permanently remove this vehicle from the inventory database.
            </div>
            
            <div class="button-group">
                <% if (typeof invData !== 'undefined' && invData) { %>
                <form action="/inv/delete" method="post" style="display: inline;">
                    <input type="hidden" name="inv_id" value="<%= invData.inv_id %>">
                    <button type="submit" class="btn-delete">
                        <i class="fas fa-trash-alt"></i>
                        sure, Delete Vehicle
                    </button>
                </form>
                <% } %>
                
                <a href="/inv/" class="btn-cancel">
                    <i class="fas fa-times"></i>
                    remove
                </a>
            </div>
        </div>
    </main>

    <!-- Add confirmation for delete form -->
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            const deleteForm = document.querySelector('form[action="/inv/delete"]');
            if (deleteForm) {
                deleteForm.addEventListener('submit', function(e) {
                    const confirmDelete = confirm('Are you absolutely sure you want to delete this vehicle? This action cannot be undone.');
                    if (!confirmDelete) {
                        e.preventDefault();
                    }
                });
            }
        });
    </script>
</body>
</html>